# Root-level example env vars (used by docker-compose)

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=p2pc

# Redis (no auth in local by default)

# coturn
TURN_REALM=p2pc.local
TURN_USER=turnuser
TURN_PASSWORD=turnpassword

